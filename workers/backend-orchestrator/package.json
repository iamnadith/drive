{
  "name": "drive-backend-orchestrator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "npm run check && wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "node scripts/deploy.mjs",
    "deploy:local": "node --env-file=.env scripts/deploy.mjs"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260825.1",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.2",
    "wrangler": "^4.126.0"
  }
}
